"Alright, business wants us to add a second image provider because their research shows cats increase user retention. Right now, the app only uses NASA APOD, but we need to integrate The Cat API for random cat photos. We'll need to create a new secret for their API key, write a new client, add a provider toggle in the config, and update the Helm charts to pass the new environment variables keeping out standards the same. And make sure the old space photos don't break."